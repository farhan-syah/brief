# Everything under .claude/ is local: session state, and the decision record whose
# evidence quotes real project names and paths from private transcripts.
.claude/

# The tools read private local transcripts and print to stdout. Nothing they emit
# belongs in git, however it is redirected.
*.log
*.jsonl
*.csv
out/

# Rust
/target/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editor and OS
.DS_Store
*.swp
*~
